# PAL handoff — 2026-05-21 (Thu evening — Memorial Day weekend gates set)

_Two-day stretch covering all work after the 2026-05-19 truck (PR #97): Wed morning sweep + Bron's payout-nudge silencer + funnel-stats endpoint + DNS auth (SPF/DMARC/DKIM all live) + Thursday's aerial photo post + Lynnette LCL listing + Memorial Day heritage spotlight queued for Monday auto-fire._

---

## Headline

Memorial Day weekend is fully scaffolded: Tuesday's beach ad push is delivering ($21+/$45 burned, 2,900+ reach, 120+ clicks), the Thursday-evening voice post landed organic-only with the `pal-aerial.jpg` aerial, and **Monday 9am CT auto-fires a quiet heritage spotlight on `/history/wwii-coastal-defenses` (post #48 queued, autoSendAt locked in)**. Funnel-stats endpoint shipped as a permanent middle-of-funnel diagnostic. SPF/DMARC/Resend-DKIM/Google-DKIM all live + verified — outbound mail now fully authenticated for the first time. Bron's payout cron no longer fires hourly nudge SMS while the revenue-split agreement is being finalized.

---

## PRs shipped this stretch (4)

| # | Title | What it does |
|---|---|---|
| #98 | fix(beach-payouts): skip manual-payouts vendors in cron | Adds `manualPayoutsOnly` flag to BeachVendor; all 3 Bron's vendors flagged. Cron silently skips them — no Stripe attempt, no "needs Connect" SMS to operator. New `skipped_manual_only` counter in response. |
| #99 | feat(wheelhouse): funnel-stats endpoint — middle-of-funnel visibility | `GET /api/wheelhouse/funnel-stats?surface=beach\|rent\|locals&hours=1..168` returns pageviews / checkout starts / completed / abandoned + rates. Wheelhouse-authed. Reusable HeyeDeploy pattern. |
| #100 | feat(shop): add Loving Coastal Living Origins to /shop | Lynnette Stirling's signup request via hello@. Listed at /shop/loving-coastal-living-origins. Reply sent via admin@ entity voice. |
| (queued only) | Memorial Day heritage post #48 | Caption + linkUrl to /history/wwii-coastal-defenses, autoSendAt=2026-05-25T14:00:00Z (Mon 9am CT). Organic only, no boost. |

Plus the FB photo post (id #47) — "you're going to know it" aerial — fired Thursday evening organic, post on PAL Page at `facebook.com/142397023082287/posts/1019627600729043`.

---

## Now live on the site

- `/shop/loving-coastal-living-origins` — Lynnette's listing
- `/api/wheelhouse/funnel-stats` — middle-of-funnel diagnostic for paid surfaces
- (existing) `/beach` with new 3-product menu + add-ons + Beach Rig (PR #92 stretch — already trucked in #97 but reads as the live baseline now)

---

## Memorial Day weekend ad push — current state

Last snapshot before truck (Thu evening ~19:00 CT):

| Channel | Cumulative spent | Reach | Clicks | CTR |
|---|---|---|---|---|
| Placed AD (`120247772162620592`) | $14.01 | 1,840 | 89 | 3.63% |
| Boost (`120247772158730592`, post#45) | $7.07 | 1,078 | 34 | 2.53% |
| **Combined** | **$21.08** | **2,918** | **123** | **~3.0%** |

Both campaigns continue through Friday PM. Approximately $24 of $45 committed remains.

**Funnel finding (worth Fri-AM re-check):** 26 /beach pageviews 24h-window, **0 checkout starts, 0 paid**. Confirmed via funnel-stats endpoint AND Winston spot-checked Stripe dashboard. Top-of-funnel-to-engagement gap, not a checkout bug. Most likely mid-week-vs-weekend timing; Friday AM data will tell if it's real form-friction or noise.

**Dispatch boost (post#43) — DOA:** denied twice by Meta auto-classifier. $0 spent, $0 reach. Letting it simmer per Winston call — no further action.

---

## Operations

### DNS authentication — all four records live + verified

| Record | Status |
|---|---|
| **SPF** `@` | `v=spf1 include:_spf.google.com include:_spf.resend.com ~all` ✓ |
| **DMARC** `_dmarc` | `v=DMARC1; p=none; rua=mailto:hello@theportalocal.com; aspf=r; adkim=r` ✓ |
| **Resend DKIM** `resend._domainkey` | RSA key (pre-existed) ✓ |
| **Google DKIM** `google._domainkey` | RSA 2048-bit key + Workspace authentication activated ✓ |

Authoritative outbound auth in place for the first time. Aggregate DMARC reports start landing at hello@ over the next ~24h. After ~2 weeks of clean reports → tighten DMARC `p=none` → `p=quarantine` → `p=reject`.

### admin@ delivery — root cause identified

Locals signup emails (and likely others) only land at hello@, not admin@. **Root cause:** admin@ is not configured as a user or alias in Google Workspace despite MX routing to Workspace + the SMTP layer accepting the address. **Fix:** Winston adds `admin@theportalocal.com` as an alias on his primary user via admin.google.com → Users + Groups → Aliases. ~60 sec, pending.

### Bron's payout cron — silenced

Hourly nudge SMS that was firing for Bron's claims past the 72hr refund window has been suppressed per PR #98. Once Bron's revenue-split agreement signs + Stripe Connect onboards, flip `manualPayoutsOnly: false` on the three Bron's vendor records and auto-payouts resume.

### Heritage / archives surface re-orientation

Caught a verify-before-declare violation today — twice — when claiming `/heritage` and then `/archives` were the heritage surface. **Actual structure:**

- **`/history`** (URL) = nav label "Heritage" = the heritage surface. 27+ long-form heritage essays already published (Tarpon Era, Lydia Ann Light, WWII coastal defenses, Hurricane Celia, Farley Boat Works, etc.). Story category `"heritage"` in `src/data/stories.ts`.
- **`/archives`** = public-domain historical photos (separate surface, 1890s-1970s era collection).
- **`/dispatch`** = investigative editorial (separate surface).

These three complement each other: stories at /history, photos at /archives, investigations at /dispatch.

The proposed "new Memorial Day heritage piece" was wrong — `/history/wwii-coastal-defenses` already covers the WWII angle. Instead, the existing piece gets the Memorial Day spotlight Monday morning.

### FB post race-fix proved out

PR #95's recovery code (query Page feed when /feed POST returns an error) held on the aerial post Thursday evening — no double-post this time. Patch works in production.

---

## Memory rules / patterns to consider filing

1. **PAL heritage = `/history` (URL), not `/heritage`.** Nav labels it "Heritage" but the route is `/history`. Tripled-up violations of `feedback_verify_before_declare.md` today — worth a memory note. Story category `"heritage"`, surface URL `/history`, nav label "Heritage" — confusing but established.
2. **Funnel-stats endpoint pattern** — reusable cross-tenant. The shape (pageviews from analytics + Stripe session count + matcher per surface) lifts directly to any HeyeDeploy tenant running Stripe Checkout. Worth promoting to a framework pattern doc when the next tenant launches.
3. **`manualPayoutsOnly` flag on BeachVendor** — small new pattern for "operator settles directly" vendor records. Same shape could apply on cart-vendors or any future on-platform vendor type.
4. **PAL `/history` discovery method** — for any future agent: `find src/app -maxdepth 2 -type d` from the repo root is the canonical "what surfaces exist" check. Don't trust shallow grep results.

---

## Tasks state at truck time

Completed this stretch: tasks #17–22, #23, #25, #26, #24.
Open pending: **#27** — fix the doubled-HERITAGE pill in `/history/[slug]/opengraph-image.tsx` OG card. Cosmetic, no urgency.

---

## Pickup for next session

### ✋ Winston actions
1. **Add admin@ as Workspace alias** at admin.google.com (~60 sec)

### 🤖 Autonomous next-session actions
1. **Friday AM funnel re-check** — `GET /api/wheelhouse/funnel-stats?surface=beach&hours=24` to see if conversion picks up as the weekend lands
2. **Verify Memorial Day spotlight auto-fires** Mon 9am CT (post #48). Sanity-check at `https://www.facebook.com/142397023082287` after fire
3. **Task #27 OG pill fix** when there's a window
4. **Watch DMARC aggregate reports** start landing at hello@ over next ~24h

### 🅿️ Parked — re-evaluate when ready
- Dispatch Part 2 (architecture piece, base on `editorial/pz-capture-draft-v4`)
- TPIA records requests (file after Wed 5/20 vote results land — vote already happened, draft ready when Winston signals)
- Heritage editorial piece — "lost-at-sea memorial tradition" angle (the unfilled gap in the /history corpus)
- Customer-file upload audience build (system addition)
- Lookalike audience (need ≥100 verified converters)
- Day-before vendor handoff-details flow (needs Winston's spec)
- Bron's revenue split (blocked on signed agreement)

---

## Verified state at truck time

- git: main HEAD = `bcb266c` (PR #98 merged). PR #99 + #100 also merged. Truck branch `chore/truck-2026-05-21` to be opened.
- `/beach/opengraph-image` returns the "Skip the haul. Skip the setup." OG
- `/history/wwii-coastal-defenses/opengraph-image` returns "The Guns That Never Fired" navy OG
- `/shop/loving-coastal-living-origins` renders the new listing
- `/api/wheelhouse/funnel-stats?surface=beach&hours=24` returns ok + pv=26
- 4 ghost ad campaigns still PAUSED (from this morning's sweep)
- Memorial Day spotlight post #48: status=pending, autoSendAt=2026-05-25T14:00:00.000Z
- Memorial Day ad campaigns ACTIVE, ~$21 spent of $45
- Bron's payout SMS suppressed (next cron tick after #98 deploy will silently skip)

---

**Filed 2026-05-21 Thu evening. Clean state on main. Memorial Day fully scaffolded. Heritage spotlight queued for Mon 9am CT auto-fire. Friday AM check pending on the funnel.**
