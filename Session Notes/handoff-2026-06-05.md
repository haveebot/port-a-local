# PAL handoff — 2026-06-05 (Fri — Workspace email migration + beach day-before comms + infra automation)

_Marathon session, two big arcs: (1) finished the Resend → Google Workspace email migration end to end and automated Vercel infra via an API token; (2) closed the beach day-before comms loop — built the missing customer confirmation + a recurring cron, made `setup_location` canonical, and fired today's live pair to Stephanie + Bron's. Plus a deep memory/branch cleanup. **8 PRs (#119–#126), all admin-merged + live.**_

## Arc 1 — Email → Google Workspace (complete)
- **Transport** (`src/lib/palEmail.ts`): **Workspace-preferred** (`admin@theportalocal.com` via Gmail SMTP / nodemailer), **Resend-fallback**. Sends + replies live in the `admin@` inbox (readable via `scripts/pal_mail.py`). Verified end-to-end — a real send landed in the inbox, not spam.
- **#121** rentals + option-A customer confirmation · **#122** the other 22 senders · **#126** the 2 SMS-forwarders (added base64 attachment support to the transport). **All PAL email now on Workspace, zero inline Resend.**
- **Vercel API token** (`VERCEL_TOKEN` in `workspace/.env`, scoped to `haveebots-projects`): Claude manages env vars + redeploys via the **REST API** (NOT the `vercel` CLI — that rule stands). Set the Workspace creds + redeployed with **zero manual config from Winston**. This is the hands-off infra pattern going forward (projectId `prj_aEfrFqLoKBLSwpQd5keNeh40Q3TR`, teamId `team_FSrdGWGTr9KzK4hrFp0ArtOF`).
- **Resend kept as the fallback** (safety net if the `admin@` app password ever rotates/breaks). The ~16 now-dead `if(!RESEND_API_KEY)` gates are harmless; a pure Workspace-only cutover (strip gates + drop `RESEND_API_KEY`) is **deferred by choice — recommend keeping the fallback.**

## Arc 2 — Beach day-before comms (complete + recurring)
- **#123 `setup_location` canonical** — was trapped in Stripe metadata; now a column on `beach_booking_claims`, captured at `/beach/confirm`, surfaced in the claim SMS + rentals tool (📍 + Send-update) + reminders. 5 upcoming bookings backfilled.
- **#124 recurring day-before cron** (`/api/cron/beach-day-before`, daily ~9am CT): finds tomorrow's claimed setups → formal **crew reminder** + **customer confirmation** (consent-gated via `sms_consent`, vendor never named), idempotent via `day_before_sent_at`. **The customer day-before confirmation didn't exist before — now it does.**
- **#125** friendly product label in day-before + reformalized the claim-confirmed SMS ("Port A Local — Claim Confirmed / you're confirmed for … / Setup location / please reach out with any questions or needs").
- **Today's live fire:** Stephanie's 6/06 pair sent manually under Winston's explicit "send" — **4 SMS** (1 to the customer, 3 to Bron's) — and stamped `day_before_sent` so the cron won't double-text.

## Other PRs
- **#119** rentals "Send update" now emails the assigned vendor alongside SMS.
- **#120** rentals tool: "Upcoming" tile + date-based auto-archive + Upcoming/Archived/All filter.

## Rule locked this session
- **No customer contact to vendors (2026-06-05):** vendor SMS/email NEVER carries the customer's phone or any direct-contact identifier — PAL owns ALL customer comms (anti-disintermediation). Vendors get the **setup spot** (job-info) + a first-name + last-initial reference. Customer SMS never names the vendor. Applies to every beach vendor comm.

## Infra / cleanup (non-PR, workspace-memory repo)
- **Memory reconciliation:** workspace-memory had a real branch divergence (unique rules on both sides) — union-merged (**0 lost**), fixed a **silent git-push failure** (`push.autoSetupRemote` was unset → memory wasn't reaching GitHub), consolidated `MEMORY.md` 34→20KB (under the load budget), restored **15 orphaned + 1 dangling HARD rule** (incl. `no_vercel_cli_calls`, the SMS-auth rule), collapsed to a single `main` branch.
- **PAL branch cleanup:** 62→4 local, 6→4 remote — deleted only verified-merged (squash-merges look "unmerged" by ancestry; verified via PR status). Kept: editorial `pz-capture` v2/v4 drafts, Collie's `og-coral-live-music` (closed PR), the worktree-pinned `feat/wheelhouse-social-direct-create`.

## Open items / decisions for next session
1. **Resend drop** (deferred): strip the ~16 dead gates + drop `RESEND_API_KEY` for pure Workspace-only — **recommend leaving the fallback.** Winston's call.
2. **Day-before cron auto-sends** (transactional, like the booking confirmation). If Winston wants per-send approval instead of auto-fire, flag + change.
3. **Beach Reassign UI** in the rentals tool — API supports it; UI is cart-only (Bron's is the sole beach vendor today).
4. **Ad relaunch** decision — measurable now via attribution + Meta CAPI; relaunch with UTM'd destination URLs.
5. **3 kept PAL remote branches** (editorial `pz-capture` v2/v4 + Collie's `og-coral-live-music`) — delete if the pieces shipped / branch abandoned (Winston's editorial call).

## State
- All code on `main` + deployed (`success`). Memory synced. Working trees clean. **Backed up from every angle.**
- Today is **Friday 2026-06-05**. Stephanie's beach setup is **tomorrow, Sat 6/06** (Bron's — day-before comms sent). Next cron fire: tomorrow ~9am CT (looks for 6/07 setups — none currently). Next real day-before: 6/12 for Ashlee's 6/13 setup.

**Filed 2026-06-05.**
