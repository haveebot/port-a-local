# PAL Handoff — 2026-05-01 (Marketing toolchain + Live mode + Bank + Ask Havee)

**State of the boat at truck:** working tree clean, main pushed, Vercel green. The marketing-tool layer of PAL went from "we have a queue" to a full consumer-grade operator product end-to-end in one session. Collie is live on the new tooling, blown away ("wow this is insane"). Live FB posting unlocked. First clean post via the queue shipped.

---

## What landed today (in commit order)

### Stocking + cadence rails (early AM)
- **Stocked queue with 10 traffic-driving FB posts** via one-shot `/tmp/stock_pal_queue.py` (live music tonight/Sat/Sun, Kite Festival 7d milestone, 4 feature spotlights — Trip Planner / Fishing Report / Live Conditions / Curated Guides — and 2 Drought Dispatch follow-up angles)
- **Sent 2 emails to Collie** from admin@: IG game-plan ask + workflow walkthrough explaining queue → review → send and what she can act on
- `2224293` **Per-post auto-fire toggle + cron** — `auto_send_at` column + Schedule UI + `/api/cron/social-auto-send` every 15 min picks up due+pending and fires
- `e9c0b6a` **Milestone auto-queue cron + CT-day floor for upcoming-list** — daily 8am CDT scan of events.ts → auto-queues 30/14/7/1d/today/wrap milestones; preview list keeps today's already-fired through end-of-day CT (Kite 1-week-out re-appeared)

### Image upload + photo mode + diagnostic
- `026cf37` **Image upload + photo mode + FB inspector** — Vercel Blob `@vercel/blob`, `/api/wheelhouse/social/upload`, `metaGraph.postToFacebook` branches on imageUrl (POST `/photos` for photo mode vs `/feed` for link card), `/api/wheelhouse/social/inspect-fb-post` Graph API diagnostic endpoint
- **Diagnosed why Collie couldn't see the post** → not a publishing issue, our app was in **dev mode** which throttles reach to admins/devs only. Reach was 2 on the drought post (vs 102+ on Collie's manual posts). Walked Winston through Meta App Live publish (Privacy/ToS URLs, app icon, category, "Manage everything on your Page" use case with `pages_read_engagement` added)
- `9a44282` **Killed root-layout `openGraph.{title,description,url}` override** — Next.js was making every page scrape with HOMEPAGE og:title + og:description + og:url despite each page setting its own. Same root-cause class as yesterday's canonical bug (commit `cfbdc9d`). Root layout now keeps only siteName/locale/type
- `0b67fa1` **Drop double "| Port A Local" suffix** — root template was applying suffix that pages already manually appended

### Queue ergonomics + "show off" UX polish
- `53d8d2d` **Reorder ↑↓ arrows** mirroring Glossary pattern + `display_order` column with backfill
- `226d336` **Jump-to-top ⤒ button** alongside arrows — sets display_order = current_min - 10
- `8dbfc49` **Position labels ("🔥 Up next · 1 of 9", "On deck", etc.) + Resend button on Recent rows** — duplicatePost helper + PATCH action="resend" + ↻ Resend on sent/skipped/failed cards
- `ca47a91` **Marketing UX overhaul** — tile-based hub at `/wheelhouse/marketing` with time-aware greeting using wheelhouse_who cookie, big tappable Social/Bank/Glossary/Ask Gully tiles, quick-action row, Coming up + Lately panels. Persistent `MarketingBreadcrumb` component (sticky, one-tap home) on social/bank/glossary/ask-gully. Hide-dev-metadata Details toggle on social cards. Friendly copy throughout.

### The Bank
- `3c6a3e1` **Image Bank — full asset library** — `pal_image_library` table + `imageUpload.ts` shared helper + `/api/wheelhouse/library` endpoints + `/wheelhouse/social/bank` page (drag-drop upload + grid + per-image edit + soft delete) + `BankPicker` modal on each social post card. Single store, two entry points (direct upload + post-card upload), both write to same catalog
- `6c7f9d6` **Empty redeploy** — pickup new BLOB_READ_WRITE_TOKEN after Winston recreated Blob store as public (initial create accidentally selected private)
- **End-to-end smoke test passed** — uploaded test PNG, verified public URL fetches anonymously, library row #1 created

### Ask Havee — NL composer
- `6d0d316` **`AskHavee` inline Claude composer** — `/api/wheelhouse/social/compose` POST with prompt, `socialComposerAgent.ts` runs Haiku 4.5 with tool-use loop (queue_post / clarify_question / decline). System prompt seeds: brand voice rules + URL inventory (built live from events.ts + dispatches + stories) + today's date in CT. UI card at top of `/wheelhouse/social`
- **Smoke-tested end-to-end** — prompt: "do a post spotlighting the trip planner — focus on how locals + return visitors use it" → drafted post #17 with on-brand caption in 2 iterations. Voice locked, URL picked correctly.

---

## Status across the operator surfaces

| Surface | State |
|---|---|
| `/wheelhouse/marketing` | LIVE — tile-based home with greeting + all marketing tiles |
| `/wheelhouse/social` | LIVE — Ask Havee composer + queue + position labels + reorder + Resend |
| `/wheelhouse/social/bank` | LIVE — drag-drop upload, grid, per-image edit, soft delete |
| BankPicker on post cards | LIVE — modal grid with search |
| `/wheelhouse/glossary` | LIVE — breadcrumb added |
| `/wheelhouse/ask-gully` | LIVE — breadcrumb added |
| FB automation | LIVE in production, **Live mode unlocked** |
| FB Live mode | LIVE 2026-05-01 |
| OG previews site-wide | CLEAN (root override fix shipped) |
| Vercel Blob | Public store, token wired, smoke-tested |
| Auto-send cron | Wired (every 15 min) |
| Milestone cron | Wired (daily 8am CDT) |
| IG automation | Stub mode (no `META_INSTAGRAM_ACCOUNT_ID` yet — emailed Collie about IG plan) |

---

## What's in the queue at truck

10 stocked + 1 from Ask Havee smoke test = **11 pending posts** (post #6 Friday live music was sent successfully but had broken OG due to root-layout bug — Collie deleted FB post and refired clean as #16):

- `#16` — Friday live music tonight (refired, **first clean post via queue, with Live mode**)
- `#7` — Saturday live music (SA Lights Journey tribute headliner)
- `#8` — Sunday slow-roll
- `#9` — Kite Festival 7d milestone (note: `event_milestone_7d:spring-kite-festival-2026` already queued so milestone cron's loose dedup will skip on next run)
- `#10` — Trip Planner spotlight
- `#11` — Fishing Report spotlight
- `#12` — Live Conditions spotlight
- `#13` — Curated Guides spotlight
- `#14` — Drought followup: Tammy King Deeport angle
- `#15` — Drought followup: 8.7% reservoir stat angle
- `#17` — **Trip Planner spotlight via Ask Havee** (composer-drafted, slightly different angle from #10)

---

## Pending threads for tomorrow's session

### High-priority cooks
1. **Stripe Issuing application** (still on the list from yesterday) — apply at https://dashboard.stripe.com/issuing/overview. ~3-7 day approval. Once enabled, build runner virtual cards + dedicated FB Ads card.
2. **Google Search Console + Bing Webmaster** — submit sitemap, request re-crawl. The single biggest near-term ranking lift now that canonical AND OG fixes are live.
3. **FB Ads strategy talk** — Winston flagged early in session, deferred. Discuss boost-organic vs dedicated-campaigns, audience, budget guardrails.
4. **Internal linking** — Related sections on Heritage + Dispatch pages using existing `relatedDispatches` / `relatedStories` fields.
5. **Schema audit** — verify the 10 schema components actually render on detail pages.

### Composer + Bank polish (next-tier)
- **Composer's clarify flow** — currently if Havee asks for clarification, the operator has to retype from scratch. Improvement: thread the previous prompt + clarify Q + new answer back into a follow-up call. ~30 min build.
- **Image dimensions extraction** — Bank stores width/height as NULL right now. Could parse PNG/JPEG/WEBP headers server-side for grid-layout improvements + portrait-vs-landscape filtering. ~30 min.
- **Tags on Bank images** — Collie said "tag what you can roll with for now" — start with auto-derived tags from filename or alt text, add manual tag UI in v2.
- **Composer tool: pick from Bank** — agent could optionally select an image from the Bank as part of the draft. Would need to give the composer agent a `pick_image_from_bank(query)` tool. Useful for "do a post about live music with a stage shot."
- **IG pipeline** — once Collie confirms IG handle + links to Page in Meta Business Suite, set `META_INSTAGRAM_ACCOUNT_ID` + everything fans out automatically.

### Ergonomic finds
- **FB Sharing Debugger force-rescrape** — can be useful to expose as a `/wheelhouse/social/[id]/refresh-fb` button for ops where we know the OG cache is stale. ~15 min.
- **Auto-send cron audit log** — when the cron auto-fires, currently sets sent_by="cron:social-auto-send". Could surface a small UI badge "auto-fired ⏱" on those cards in the Recent list. ~10 min.
- **Add `pages_read_engagement` to Meta App token scope** — would unlock the FB inspector endpoint to actually return data instead of (#10) error. Optional — only matters if we want to check engagement metrics back into the Wheelhouse.

### Memory updates worth landing
- `feedback_consumer_ux_for_non_tech_operators.md` — DONE, mirrored
- `feedback_heyedeploy_pattern_thinking.md` — DONE, 11 new templates added (Ask Havee composer, Image Bank, marketing tile hub + breadcrumb, auto-send cron, milestone cron, position/reorder/jump-to-top, Resend, FB photo mode + Bank upload, Hide-dev-metadata toggle, FB inspector, Meta dev-mode reach diagnostic)
- Plus the 2026-05-01 cross-project diagnostics section: dev-mode reach throttle, root-layout openGraph override, Vercel Blob private-vs-public, "two systems with similar names" gotcha, UX-as-feature-jump validation

---

## Quick-start prompt for next session

> Arnold for PAL. Read this handoff at `Session Notes/handoff-2026-05-01.md` first. Then: do a wheelhouse orient. Surface state in one paragraph.
>
> The big news from yesterday: marketing toolchain shipped end-to-end + Collie is live on the new UX. Live mode unlocked on Meta App. First clean post via the queue (#16) confirmed working. Ask Havee composer working. Bank operational. ~11 posts pending in queue ready to fire.
>
> Three top open threads: **(1)** Stripe Issuing application, **(2)** Google Search Console + Bing Webmaster sitemap submission (canonical + OG fixes both done so this is the next biggest ranking lift), **(3)** FB Ads strategy talk — Winston flagged early in 2026-05-01 session, deferred.

---

## Heavy-truck note: HeyeDeploy templates locked today

Adding these to the HeyeDeploy template catalog (per `feedback_heyedeploy_pattern_thinking.md`):

1. **Inline Claude content composer (Ask Havee pattern)** — extends the SMS Havee agent (2026-04-29) to a different surface; same tool-use loop pattern, different tools
2. **Image Bank — single asset library with multi-entry-point upload** — Vercel Blob + Postgres catalog + soft delete
3. **Marketing tile-based hub + breadcrumb** — consumer-app UX for non-tech operators, see `feedback_consumer_ux_for_non_tech_operators.md`
4. **Per-post auto-send cron + Schedule UI** — operator-set fire time per item
5. **Calendar-based milestone auto-queue cron** — daily scan for time-anchored content auto-queue
6. **Position labels + reorder arrows + jump-to-top** — operator queue ergonomics
7. **Resend pattern** — duplicate sent rows back to pending
8. **FB photo mode + library-cataloged upload** — image-upload-to-cataloged-blob pattern
9. **Hide-dev-metadata + Details toggle** — for non-tech operator data cards
10. **FB inspector diagnostic endpoint** — for "why can't X see this post" cases
11. **Meta dev-mode reach throttle diagnostic class** — cross-project rule for any Meta Graph integration

Plus 4 cross-project rules from today's diagnostics:
- Meta App Live mode required for full reach (dev mode = admin/dev/test-user only)
- Root-layout `openGraph.{title,description,url}` overrides child pages (same as canonical bug)
- Vercel Blob: token auto-injects, public store required for FB-scrape, redeploy after store recreation
- "Two systems with similar names" UX gotcha (preview list vs active list — distinguish visually)
- UX-as-feature-jump validation — for non-engineer collaborators, polish IS a feature

---

*Trucked clean. Worktree clean. Pushed to origin/main. Vercel green. Memory mirror synced (23 files). Ready for tomorrow.*
