# Session Notes — April 14, 2026

## Dispatch launches. Lighthouse becomes the PAL brand. Media-outlet posture locked.

---

## Port A Dispatch — new editorial section
- New route `/dispatch` — distinct from Heritage (preserved history). This is current-events journalism.
- Data layer: `src/data/dispatches.ts` + `src/data/dispatch-content.ts` mirroring the heritage pattern.
- NewsArticle JSON-LD schema, dated bylines, signature seal footer on each piece.
- Sitemap + Gully index updated to include Dispatch.

### Dispatch #1 — "The Two Port Aransases" (2,050 words, live)
- Tourism Bureau ("Island Life/Beach Nation") vs. in-town economic reality.
- Sonic closure, Cinnamon Shore / Palmilla / Sunflower Beach as the 30A template (Mark Schnell connection).
- Sales tax vs HOT tax divergence, PAISD flat enrollment.
- Fully sourced — Comptroller data, South Jetty coverage, developer press releases.
- See [[feedback_pal_dispatch_workflow]] in workspace memory — the 6-step pattern validated this session.

### Editorial workflow validated
1. Push back on the brief first.
2. Verify every factual claim publicly.
3. Hold back unverifiable claims as follow-ups.
4. Critique roles, not people.
5. Ship directly to live when authorized.
6. Build architecture, not just content.

Dispatch #2 "The Landlord Nobody Voted For" queued (needs tenant sourcing before it can ship).

---

## Brand — the Lighthouse mark
- **Three directions explored first**, then v2 fusion, then v3 Lighthouse-only (the locked direction).
- Inspired by the **Lydia Ann Light** — anchored in island history, not generic beach iconography.
- One `LighthouseMark` component, four detail levels:
  - `full` — beam rays, finial, windows, 3-panel shading. Hero/OG/watermark (128px+).
  - `standard` — default nav + dispatch footer (40–96px).
  - `simple` — flat silhouette + coral light. Print, mobile (32–48px).
  - `icon` — pure silhouette for favicon/buttons (12–32px).
  - `monochrome` prop for single-ink prints/merch.
- Static SVGs shipped: `public/logos/lighthouse-{full,standard,simple,icon}.svg`.

## Full-site brand saturation (commit 169a729)
Every surface now carries PAL identity:
- Favicon (`icon.svg`) + Apple touch icon (`apple-icon.tsx`).
- Organization + WebSite JSON-LD in root layout (Google knowledge-panel logo).
- PWA manifest (standalone, PAL icons, navy theme, Add to Home Screen).
- `metadataBase` + title template `%s | Port A Local`, canonical URLs, themeColor meta.
- Homepage hero: 620px lighthouse watermark + 14px badge icon + coordinates.
- Footer: lighthouse lockup, 420px corner watermark, coordinates masthead strip (27°50′N · 97°03′W · Mustang Island · Est. 2026 · Palm Family Ventures, LLC).
- Nav: lighthouse beside wordmark on every page, subtle hover rotation.
- 404: 80px foreground + 560px watermark, "Off the Chart" copy, Gully It CTA.
- **OG share cards on 17+ routes** via shared `brandedOG` helper (`src/lib/brandedOG.tsx`).
- **Branded transactional emails** via shared `emailLayout` helper (`src/lib/emailLayout.ts`) — all 3 portals + priority dispatch + suggestion admin alerts.

---

## Anti-Bureau positioning locked
- Dispatch #1 established the posture; brand reinforces it visually: serif weight, navy+coral (not aqua/yellow), coordinates (not city name), journalistic voice.
- PAL is positioned as the media/editorial counter-narrative to the Tourism Bureau's destination marketing.

## Commits (6)
- `f955304` Dispatch: new editorial section + first piece "The Two Port Aransases"
- `2cc22ae` Brand: three lighthouse mark directions + preview page
- `90223ed` Brand v2: fusion mark (D1+D3) with anti-bureau attitude
- `4886119` Brand v3: Lighthouse-only system, four detail levels, in-use mockups
- `3beb9e3` Brand: wire lighthouse end-to-end across the site
- `169a729` Brand: full-site saturation — no surface without PAL identity
