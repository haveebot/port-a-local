# PAL Handoff — 2026-04-27

_Use this to pick up cold in the next session. Read top to bottom, then say "ready"._

---

## What just shipped (this session, day 2 of the marathon)

**Theme:** "the full drill" — closing automation + findability gaps for the two new portals (`/housekeeping` and locals sell-mode) that went live Sprint 3 but lacked the cascades needed to actually function end-to-end.

**Last commit:** `134619b` — "PAL: full drill — sell-mode + housekeeping email cascades + discoverability"

**What landed:**
1. **Locals sell-mode purchase email cascade.** New `locals_purchases` ledger (Stripe session ID PK, `emails_sent_at` gate). Three emails fire on first load of `/locals/buy-success/[listingId]`: vendor ("you sold X"), customer (receipt + what to expect), admin (audit). Wheelhouse mirror appends each sale to the locals thread. Vendor email falls back to admin@ when `vendorEmail` missing. Idempotent — refresh is a no-op.
2. **Housekeeping confirmation cascade.** `sendHousekeepingCustomerEmail` + `mirrorHousekeepingBookingToWheelhouse`. Both fire only on `placed → paid` transition (60s grace window — refresh-safe).
3. **Discoverability for `/housekeeping`.** Added to Gully index, mobile nav under Services vertical, `/services` landing now a 4-portal grid, cross-link boxes on `/maintenance` and scope-footer of `/locals`.
4. **Earlier failed deploy diagnosed and confirmed self-healed.** Was `4bgrpgtbd` from commit `cf5f2f6` (TS narrowing on `CreateOfferInput.mode`). Fixed in next commit `707970c`. Production has been Ready since.

**Files added:**
- `src/lib/localsBuyEmails.ts` — vendor/customer/admin senders for sell-mode purchases
- `src/lib/housekeepingDispatch.ts` — Wheelhouse mirror for paid bookings

**Files modified:**
- `src/data/locals-types.ts` (added `vendorEmail`/`vendorPhone` on Listing)
- `src/data/locals-store.ts` (added `locals_purchases` table + `createOrGetLocalsPurchase`/`getLocalsPurchase`/`markLocalsPurchaseEmailsSent`)
- `src/app/locals/buy-success/[listingId]/page.tsx` (wired idempotent cascade + Wheelhouse mirror)
- `src/app/locals/page.tsx` (cross-link to `/housekeeping`)
- `src/lib/housekeepingEmails.ts` (added `sendHousekeepingCustomerEmail`)
- `src/app/housekeeping/success/[bookingId]/page.tsx` (wired customer email + mirror with 60s grace gate)
- `src/lib/gullySearch.ts` (added `/housekeeping` portal entry)
- `src/components/Navigation.tsx` (added `/housekeeping` to mobile drawer)
- `src/app/services/page.tsx` (4-portal grid)
- `src/app/maintenance/page.tsx` (cross-link box for cleaning)

---

## What's awaiting Winston (action required)

1. **Tyler — photo verification.** Tyler is approved + live on /locals (rent mode) but photos haven't landed yet. Ping him for photos when convenient; admin verifies via existing magic-link flow. Per the new "verify = live, photos = optional optimization" rule this is NOT a gate — just a quality-surface optimization.
2. **Local Girls Cleaning brand review (Collie).** Brand placeholder for /housekeeping. Surfaces using it: page hero, success page, Stripe checkout product description. Easy swap once Collie locks the actual brand.
3. **License-plate field on existing runners.** Schema column added but only new signups fill it. Existing runners need a backfill prompt — could be magic-link flow, or just "next time you sign in we'll ask." Decide approach.
4. **First sell-mode listing.** `LISTINGS` array in `src/data/locals-listings.ts` is still empty. Cascade is built and tested in build but never run live. Once Tyler-equivalent for sell-mode signs up, add the listing manually (with `vendorEmail` + ideally `stripeAccountId` from a Stripe Connect onboarding flow that doesn't exist yet — see deferred below).
5. **Lowe's Market data verification.** Address `611 N Alister St`, phone `(361) 749-6602`, 7am-10pm hours are best-guesses from earlier session. Confirm on first run.
6. **A2P 10DLC clearance.** Pending Twilio approval. Blocks SMS notifications on every flow (delivery, locals sell, housekeeping). Email-only until cleared.

---

## Deferred design / next-rocks ranked

| Rock | Leverage | Time | Notes |
|------|----------|------|-------|
| **Stripe Connect onboarding for sell-mode vendors** | High — unblocks real vendor payouts | 1-2 hr | Today's cascade falls back to "PAL holds funds, manual payout" if vendor has no `stripeAccountId`. Build the magic-link Connect onboarding page (mirror the runner flow). After this, vendors get paid automatically on every sale. |
| **Sell-mode offer-to-listing pipeline** | Medium-high | 2-3 hr | Right now offers go to DB, then Winston manually adds to `LISTINGS` array. Build a Wheelhouse "promote to listing" button on the offer detail page so approved offers automatically become live listings. |
| **Validate locals-purchase cascade live** | High (de-risk) | 30 min | Need at least one sell-mode listing in `LISTINGS` to actually run a test buy. Could seed a `test-listing` row gated on env var. |
| **Wheelhouse Pulse integration for new portals** | Medium | 30 min | Housekeeping bookings + locals sales should appear in the Pulse activity feed on /wheelhouse. Mirror is firing, just need to surface in Pulse aggregator. |
| **Homepage surface for housekeeping** | Medium | 30 min | Add Why-PAL tile or Services cluster mention on `/`. Currently only reachable through nav + Gully + cross-links. |
| **Housekeeping marketplace v2** | Low (premature) | Days | Claim-first-wins fan-out to local cleaners, like cart-rentals. Wait until v1 has 10+ bookings under its belt. |
| **Order modification + runner ↔ customer comms (Phase 1)** | Medium | 2 hr | Direct phone-to-phone + refund-only adjust. Doc: `Port A Local/Features/Order Modification + Runner-Customer Comms — Design.md`. |
| **Convenience-store rollout (more stores)** | Medium | per-store | Lowe's Market is the proof. Pattern is locked. Add Stripes/H-E-B/CVS one at a time as Winston gets relationships. |
| **Runner Rewards Tier 2-4** | Deferred | — | Doc: `Port A Local/Features/Runner Rewards Program — Design.md`. Tier 1 ($5 first delivery) is live. |

---

## Recent decisions (not yet promoted to project memory file body)

- **PAL doesn't gatekeep — north star.** When deciding require/restrict/filter, default to "let them in." Quality is surfaced (cost of low quality is made visible) not enforced. Source: `feedback_pal_doesnt_gatekeep.md`. Already promoted.
- **Verify = live for /locals.** Approval makes a listing live; photos are an optional optimization not a gate. Subject line "You're in" not "You've been verified." Already promoted.
- **Outward-facing stays agnostic of "a PAL service."** Internal code comments retain the framing. Public surfaces are inclusive. Caught + fixed in commit `cf5f2f6`. Promoted.
- **Cross-project context-handoff protocol.** This file is the first execution. Source: `feedback_context_handoff.md`. Promoted.
- **10% flat customer-pays platform fee.** Locked across all PAL verticals (delivery, /locals sell, /housekeeping). Customers carry the platform's economics, vendors keep 100% of their quote.
- **CityDeploy = Heye Lab's SaaS-ified PAL.** Naming locked. Tagline: "Powered by Heye Lab · Built on CityDeploy." Promoted.

---

## Direct quotes from Winston (this session, intent signals)

> "is this completely clickable and findable in the site? - have we automated everything we can with at our current status/pre a2p? run the full drill if we can"

> "let's remove the 'A PAL service' - that is internal - outward facing we are all incluive and agnostic"

> "they are in, they are verified - they just still should submit pics... PAL DOESN'T GATEKEEP"

> "do we draft or do we cook - do we wait for input and suggestions or do we ask for a fix... do we max or take our time"

> "we had a deployment fail a little while ago for / housekeeping" (this was the 50m-old `4bgrpgtbd` — already self-healed; reported back)

> "let's gooooo chef! what do we tackle next, we're hot. need a truck?" — handoff trigger

---

## Quick-restart command

Drop this into a fresh PAL chat:

> Pick up from `Port A Local/Session Notes/handoff-2026-04-27.md` — read that first. Then run `python scripts/wheelhouse.py orient` to get current state. Ready when you say.

---

## Verification before next session starts

```bash
# Working tree should be clean
cd /Users/winstoncaraker/Projects/workspace/port-a-local && git status

# Latest commit on origin/main should be 134619b
git log --oneline origin/main | head -3

# Production should be Ready (latest deploy: j9kdbvjnz)
vercel ls 2>&1 | head -4

# Smoke test the four touched routes
curl -s -o /dev/null -w "/housekeeping → %{http_code}\n" https://theportalocal.com/housekeeping
curl -s -o /dev/null -w "/locals → %{http_code}\n" https://theportalocal.com/locals
curl -s -o /dev/null -w "/services → %{http_code}\n" https://theportalocal.com/services
curl -s -o /dev/null -w "/maintenance → %{http_code}\n" https://theportalocal.com/maintenance
```

— The Port A Local
